# Comma-separated OAuth providers enabled in the login screen (default: discord)
VITE_PROVIDERS=discord,google,facebook
# API base URL if the frontend is served from a different origin than /api
# (defaults to same-origin, which Vite proxies in dev).
#VITE_API_URL=http://localhost:4000
